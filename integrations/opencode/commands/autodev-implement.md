---
description: Implement one AutoDev issue through the privacy-gated Python role runner
agent: build
subtask: false
---
The deterministic AutoDev Python role runner executes the isolated Implementer before this display-only prompt is sent:

!`autodev role --role implementer --arguments "$ARGUMENTS" --interactive-consent`

Return only the final JSON object above. Do not call tools, read repository files, continue to another role, or infer workflow state.
