{
  "$schema": "https://opencode.ai/config.json",

  // One OpenCode model for the coordinator and every unmapped AutoDev child role.
  // Replace the placeholder with a real provider/model-id configured in OpenCode.
  "model": "provider/<model-id>"
}
